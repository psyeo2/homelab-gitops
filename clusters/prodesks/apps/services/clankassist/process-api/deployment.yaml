apiVersion: apps/v1
kind: Deployment
metadata:
  name: clankassist-process-api
  namespace: apps
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: clankassist-process-api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: clankassist-process-api
    spec:
      containers:
        - name: process-api
          image: 192.168.1.57/homelab/process-api:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 3001
          envFrom:
            - configMapRef:
                name: clankassist-process-api
          readinessProbe:
            tcpSocket:
              port: http
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: http
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
